# Shell scripts must keep LF endings in the working copy. With core.autocrlf=true
# (the Git for Windows default) a checkout would otherwise rewrite them to CRLF, and
# bash then fails on every line with: $'\r': command not found.
*.sh text eol=lf
